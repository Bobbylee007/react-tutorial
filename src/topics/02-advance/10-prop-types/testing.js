
// rafcp for propType component
import React from 'react'
import PropTypes from 'prop-types'

// rafcp for propType component

const testing = props => {
  return (
    <div>   
    </div>
  )
}

testing.propTypes = {
// short-cut for setting prop  EX: { name:ptar}
name:PropTypes.array.isRequired,

}

export default testing
